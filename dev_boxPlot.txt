Features to Add:
  NameValueArgs.boxOrientation {mustBeMember(NameValueArgs.boxOrientation,['vertical','horizontal'])} = 'vertical'
  Jitter (outliers and points/lines) based on kernel density
  Legend background color
  Legend box color
  Legend font color
  NameValueArgs.lgdFontWeight {mustBeMember(NameValueArgs.lgdFontWeight,['normal','bold'])} = 'normal'
  Legend font angle

Features to Consider:
  Differentiate whisker cap from whisker line (color, width, style, alpha)

Issues to Resolve:
  Account for fully specified pointColors/lineColors (matching nSamples)
